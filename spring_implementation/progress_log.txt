====================================================
  Research Progress Log — C&S BIO 199 / 197
  PI: Elisa Franco  |  Student: Daniel Chang
====================================================

----------------------------------------------------
2026-04-22  Spring 2026 — Segmentation Model Survey
----------------------------------------------------

CONTEXT
  Building on the Winter 2026 automated Cellpose pipeline
  (ROI PC = 2.04, reference PC = 6.32). Goal is to find a
  better segmentation approach to close the PC gap and
  package the pipeline for lab-wide use.

WHAT WAS DONE
  Tested four new segmentation models on the JABr ROI dataset
  (C2-ROI_raw_stack_sample2_5.tif, 55 x 185 x 259, 2-channel):

  Model               Approach                      PC      Runtime
  ------------------  ----------------------------  ------  -------
  Old Cellpose (2D)   cyto2, slice-by-slice          2.04    —
  Cellpose 3          cyto3 + denoising + 3D mode    3.33    11 s
  StarDist 2D         versatile_fluo, slice-by-slice 1.91     6 s
  U-FISH              ONNX spot detection             3.81     2 s
  Nellie              Frangi filter + graph analysis  3.57    14 s
  Reference value     manual/ImageJ benchmark         6.32    —

KEY OBSERVATIONS
  - Cellpose 3 (do_3D=True + DenoiseModel) is the strongest
    principled improvement over the old pipeline — better boundary
    tightness, fewer slice-stitching artifacts.
  - U-FISH has the highest PC and fastest runtime but treats
    condensates as point sources (spot detection), not filled
    objects. PC may be inflated by cherry-picking bright spot
    centers rather than full condensate volume.
  - StarDist underperformed (PC < old baseline) — the
    versatile_fluo model is trained on cell nuclei, not small
    condensate puncta.
  - Nellie is designed for tubular organelles (mitochondria);
    results are reasonable but not biologically motivated here.
  - All new models still fall short of the reference PC = 6.32.
    Gap is likely due to condensate segmentation sensitivity —
    small boundary errors have a large effect on intensity ratios.

OUTPUTS
  segmentation_test/
    cellpose3_segmentation.py
    stardist_segmentation.py
    ufish_segmentation.py
    nellie_segmentation.py
    run_comparison.py          <- runs all models + generates figures
    outputs/comparison/
      comparison_figure.png   <- side-by-side segmentation + PC + runtime
      flowmap_figure.png      <- pipeline diagram for each model
      raw_condensate_midZ.png <- reference raw image

CELLPOSE 3 vs OLD CELLPOSE — KEY DIFFERENCES
  Three concrete changes account for the PC improvement (2.04 → 3.33):

  1. Denoising (new)
     Cellpose 3 runs DenoiseModel cyto3 on every slice before
     segmentation, suppressing shot noise and sharpening condensate
     boundaries. The old pipeline segments the raw image directly
     with no preprocessing. Note: PC intensities are always computed
     from the original raw pixels — denoising only guides boundary
     placement, not the intensity values used in the ratio.

  2. 3D mode (new)
     Cellpose 3 uses do_3D=True, processing XY, XZ, and YZ planes
     simultaneously and merging gradient flows across all three before
     drawing boundaries. The old pipeline runs 2D segmentation
     independently on each of the 55 Z-slices with no cross-slice
     awareness. A condensate spanning 3 slices gets segmented 3
     separate times, often with inconsistent boundaries per slice —
     directly hurting the intensity ratio.

  3. Model version: cyto3 vs cyto2
     cyto3 is trained on a larger and more diverse dataset than cyto2,
     and is generally better at small, dim, or irregularly shaped
     objects. The 3D mode is likely the largest single contributor
     to the PC improvement.

PC COMPARABILITY NOTE
  The PC formula is identical across all models:
    cond_pixels   = Ch2[ cond_mask & nuc_mask ]   (inside condensate AND nucleus)
    dilute_pixels = Ch2[ nuc_mask & ~cond_mask ]  (inside nucleus, outside condensate)
    PC = mean(cond_pixels) / mean(dilute_pixels)

  However, the masks feeding into this formula differ:

  Model            Condensate mask         Nuclei mask
  ---------------  ----------------------  ----------------------
  Old Cellpose     Cellpose cyto2 (2D)     Cellpose cyto2 (2D)
  Cellpose 3       Cellpose cyto3 (3D)     Cellpose cyto3 (3D)
  StarDist         StarDist2D              StarDist2D
  U-FISH           U-FISH spot + disk      Otsu threshold (!)
  Nellie           Frangi + graph          Frangi + graph

  U-FISH is the outlier — it uses a simple Otsu threshold with
  morphological closing for the nuclei mask rather than a trained
  model. This gives it a different nuclear boundary than the others,
  directly affecting which pixels count as dilute-phase and therefore
  the PC. Its high PC (3.81) should be interpreted with this caveat.

  For a truly fair comparison, all models should use the same nuclear
  mask (e.g. Cellpose 3 nuclei masks) and only swap the condensate
  mask — isolating condensate segmentation as the single variable.

NEXT STEPS
  - Improve condensate segmentation quality (tighter boundaries)
    to push PC closer to reference.
  - Implement 3D volume estimation (aggregate masks across Z).
  - Package pipeline with CLI arguments and config file.
  - Write Spring 2026 poster / report.

----------------------------------------------------
2026-04-23  Reference PC Investigation
----------------------------------------------------

CONTEXT
  Reviewed the two manual reference CSV files (JABr_Partition
  coefficient_nuclear.csv and JABr_Partition coefficient_cytoplasmic.csv)
  for the ROI sample: 20240516_JABr_40uMDFHBI_20-40_Sample2_5_1.tif

RAW VALUES FOR Sample2_5_1
  Nuclear:
    condensate density : 658.30
    dilute density     : 104.09   <- anomalously high (see below)
    nuclear PC         : 6.324    <- the reference value used all along

  Cytoplasmic:
    condensate density : 411.70
    dilute density     :  85.14
    cytoplasmic PC     : 4.836

FINDING 1 — Elevated dilute density
  The nuclear dilute density for Sample2_5_1 (104.09) is 30-35 units
  higher than every other Sample2_5 entry (range: 68-73). If the dilute
  density matched neighbors (~70), the reference nuclear PC would be
  ~658/70 = 9.4 instead of 6.32. This suggests the reference value
  may be understated, not overstated.

  Possible cause: the manual ImageJ condensate mask for this ROI was
  slightly loose, leaving bright condensate-edge pixels counted as
  dilute-phase background, raising the dilute density and deflating PC.

FINDING 2 — Background subtraction not in automated pipeline
  The person who computed the manual reference CSVs confirmed she applied
  background subtraction before computing PC. The automated pipeline
  does NOT do this. Background subtraction inflates PC because it hits
  the denominator disproportionately:

    PC_bg = (condensate_density - B) / (dilute_density - B)

  Since dilute density is much closer to background than condensate
  density, subtracting B lowers the denominator more (proportionally),
  pushing PC up. This means the reference PC = 6.32 and the pipeline
  PC = 3.33 (Cellpose 3) are NOT computed the same way — part of the
  gap is methodological, not just segmentation quality.

  Need to confirm: what region was used to estimate background B?
  (outside all cells, or something else?)

WHAT PC THE CURRENT PIPELINE COMPUTES
  The automated pipeline computes nuclear PC, raw (no background subtraction):

    cond_pixels   = Ch2[ cond_mask & nuc_mask ]    (inside condensate AND nucleus)
    dilute_pixels = Ch2[ nuc_mask & ~cond_mask ]   (inside nucleus, outside condensates)
    PC = mean(cond_pixels) / mean(dilute_pixels)

  This is equivalent to the nuclear PC column in the reference CSV,
  EXCEPT the reference also applies background subtraction.

TYPES OF PC (for clarity)
  Nuclear PC      : condensate vs. dilute phase, both measured inside the nucleus.
                    What the pipeline computes. Reference = 6.32 (bg-subtracted).
  Cytoplasmic PC  : condensate vs. dilute phase, both measured in the cytoplasm.
                    Reference = 4.84. Pipeline does not currently compute this.
  Bg-subtracted PC: same formula but with a background constant B (measured
                    outside all cells) subtracted from both densities before
                    the ratio. Inflates PC relative to raw. Reference uses this.

ACTION ITEMS
  - Confirm background estimation method with Shiyi who did the manual analysis.
  - Add background subtraction to the automated pipeline to match the reference method.
  - Recompute PC with bg subtraction and compare to reference 6.32 directly.

  Fluor Dens = summation of background subtracted voxel intensities / number of voxels

----------------------------------------------------
2026-04-25  PI Meeting — Background Subtraction Confirmed
----------------------------------------------------

CONTEXT
  Meeting with PI Elisa Franco. She provided the reference PC calculation
  method from the Fabrini et al. manuscript (section 1.8.4).

REFERENCE PC FORMULA (Fabrini et al., 1.8.4)
  PC = Fluor. density_condensed / Fluor. density_dilute

  Fluor. density_condensed:
    1. B = minimum voxel intensity across the field of view
    2. Mask condensate region (inside condensate ∩ nucleus)
    3. Subtract B from each voxel, clip to 0
    4. Sum all background-subtracted voxel intensities
    5. Divide by total number of voxels in the mask
    -> = Σ clip(pixel - B, 0) / n_voxels_in_mask

  Fluor. density_dilute:
    1. Same B (min voxel intensity across FOV)
    2. Crop a 10×10×10 voxel patch from the dilute phase
       (inside nucleus, outside condensates)
    3. Subtract B, clip to 0
    4. Mean of the patch
    -> = mean clip(pixel - B, 0) over the 10×10×10 patch

IMPLEMENTATION
  Updated cellpose3_segmentation.py Step 4 to match this formula.
  Key changes:
    - B = cond_stack.min() (minimum voxel across full 3D FOV)
    - Condensed density: vectorized over all 3D nuclear condensate voxels
    - Dilute density: searches for a valid 10×10×10 patch entirely within
      the nuclear dilute phase (rng seed=42 for reproducibility);
      falls back to all dilute pixels if no patch found
    - Removed the old slice-by-slice loop for PC; kept Cellpose 3 (do_3D)
      as the segmentation model

DECISION
  Cellpose 3 (cyto3 + DenoiseModel + do_3D=True) stays as the pipeline
  model. The bg-subtraction update is the only PC formula change needed.
  Will rerun to get updated PC vs. reference 6.32.

IMPLEMENTATION — ALL MODELS UPDATED (2026-04-25)
  Applied the same background-subtracted PC formula to all five scripts:
    segmentation_test/cellpose3_segmentation.py   (Step 4)
    segmentation_test/stardist_segmentation.py    (Partition coefficient block)
    segmentation_test/ufish_segmentation.py       (Partition coefficient block)
    segmentation_test/nellie_segmentation.py      (Partition coefficient block)
    segmentation_test/run_comparison.py           (old_cellpose inline PC block)

  Changes in each:
    - B = cond_stack.min() computed before the PC block
    - Condensed density: vectorized 3D, clip(pixel-B, 0), sum / n_voxels
    - Dilute density: 10×10×10 patch search (rng seed=42), fallback to all dilute pixels
    - Summary CSVs updated to include "background" row
    - Print statements updated to label values clearly

  All five scripts now compute PC on the same methodological basis,
  enabling a fair apples-to-apples comparison across models.

NEXT STEPS
  - Run all scripts and record new bg-subtracted PCs for each model
  - Update model comparison table in CLAUDE.md with new values
  - 3D volume estimation
  - CLI packaging

----------------------------------------------------
2026-04-28  Spring Pipeline Built + Nuclei Over-Segmentation Fix
----------------------------------------------------

CONTEXT
  Week 5 check-in. spring_implementation/pipeline.py is the new
  production pipeline built from scratch this quarter. It incorporates
  all improvements identified in the model survey and reference PC
  investigation.

WHAT WAS DONE

  1. Built spring_implementation/pipeline.py
     Full CLI pipeline with these steps:
       [1/6] Load condensate + nuclei TIF stacks
       [2/6] Denoise both channels with Cellpose 3 DenoiseModel (cyto3)
       [3/6] Segment with Cellpose 3 (cyto3, do_3D=True)
       [4/6] Per-slice regionprops: area, centroid, mean_intensity per Z
       [5/6] 3D volume estimation: sum per-label voxels across all slices
       [6/6] Background-subtracted PC (Fabrini method, rng seed=42)
     Outputs: masks (TIF), measurements (CSV), volumes (CSV),
              summary (CSV), results.png

     PC on JABr ROI: 6.775 (reference = 6.32) — essentially matching
     the manual reference for the first time.

  2. Nuclei over-segmentation identified
     Inspected Glasbey LUT visualization of nuclei masks. Large nuclei
     with internal texture are being split into multiple fragments by
     Cellpose — e.g. one nucleus gets labels brown + pink, another gets
     green + cyan + yellow + red. These gaps between fragments corrupt
     the PC calculation:
       - Condensates in gaps are excluded from nuclear PC
       - 10x10x10 dilute patch candidates that overlap gaps are rejected
     This means the current PC = 6.775 may be incorrect.

  3. Fix implemented in pipeline.py
     Split segment_3d() into two separate functions:
       segment_condensates() — unchanged defaults
       segment_nuclei()      — added cellprob_threshold and explicit diameter
     Added two new CLI args:
       --nuc-diameter   (default: 60px)
       --nuc-cellprob   (default: -2.0, lower = more merging)
     Condensate segmentation is unaffected.

  4. Speaker notes written
     spring_implementation/speaker_notes.md now has notes for all 10
     slides in week_5_slides.pdf.

TODO — NEEDS TO BE DONE ON GPU PC
  - Run pipeline.py with new --nuc-diameter and --nuc-cellprob defaults
    on the JABr ROI data (C1 + C2 TIF files in data/)
  - Check the nuclei Glasbey visualization to confirm splits are gone
    (should see ~3 full nuclei as single-color regions per slide 7 image)
  - If still splitting: try --nuc-cellprob -3 or -4, or increase
    --nuc-diameter to 80
  - Record the new PC after nuclei fix and compare to 6.775 and 6.32
  - Update progress log with results

COMMAND TO RUN
  python spring_implementation/pipeline.py \
    --cond data/raw_condensates/C2-ROI_raw_stack_sample2_5.tif \
    --nuc  data/raw_nuclei/C1-ROI_raw_stack_sample2_5.tif \
    --output spring_implementation/outputs

----------------------------------------------------
2026-04-29  Nuclei Parameter Sweep + nuclei Model Test
----------------------------------------------------

CONTEXT
  Following the nuclei over-segmentation issue identified on 2026-04-28,
  ran a parameter sweep on spring_implementation/pipeline.py and tested
  a separate model_type="nuclei" in nuclei_model_test/test_nuclei_model.py.
  Goal: find the setting that reduces over-segmentation and keeps PC
  closest to reference 6.32.

ROOT CAUSE OF REGRESSION
  The new segment_nuclei() function introduced on 2026-04-28 set
  --nuc-diameter default to 60px. This is too small — the actual nuclei
  span 100px+ in this ROI. Cellpose interprets the forced small diameter
  as instructions to find many small objects, fragmenting each nucleus
  into ~33 pieces (103 total). This broke the nuclear binary mask coverage
  and dropped PC to 2.134.
  Fix: revert --nuc-diameter default to None (auto-detect).

PARAMETER SWEEP RESULTS (spring_implementation/pipeline.py)
  All runs: model_type=cyto3, do_3D=True, diameter=None unless noted.

  --nuc-cellprob  --nuc-diameter  Nuclei found  PC
  --------------  --------------  ------------  -----
  -6              None            160           5.261
  -2              None             76           6.048  <- best match to ref
  0 (CP default)  None             80           6.775  <- original result
  -2              60px            103           2.134  <- regression (bad diam)
  -4              80px            100           3.180  <- still bad diam

  cellprob direction note: lower values produce MORE objects on this data
  (opposite of the "lower = more merging" assumption in the code comment).
  Likely because lower threshold includes more background pixels as cell,
  adding spurious detections rather than merging fragments.

NUCLEI MODEL TEST (nuclei_model_test/test_nuclei_model.py)
  Tested model_type="nuclei" (dedicated nucleus segmentation model) vs
  cyto3 for the nuclei pass. Condensate segmentation unchanged (cyto3).

  Model          Nuclei found  PC
  -------------  ------------  -----
  cyto3 (best)    76           6.048
  nuclei model   109           6.922

  The nuclei model performed worse: more over-segmentation (109 vs 76)
  and PC farther from reference. The nuclei model is tuned for clean
  DAPI/Hoechst images; the JABr nuclei channel contains fluorescent
  aptamer signal with internal condensate texture, which confuses it.

BEST CONFIGURATION (updated pipeline.py defaults)
  Model:         cyto3  (both condensates and nuclei)
  do_3D:         True
  --diameter:    None  (condensate auto-detect)
  --nuc-diameter: None  (nuclei auto-detect)   <- changed from 60
  --nuc-cellprob: -2.0

  PC = 6.048  (reference = 6.32, error = -4.3%)
  Nuclei objects: 76  (still visually over-segmented, but binary mask
  coverage is correct — PC formula uses nuc_masks > 0, not label count)

OUTSTANDING ISSUE
  Nuclei are still visually split into ~25 fragments each. True fix
  would require either: (a) 2D slice-by-slice + stitch_threshold for
  nuclei, or (b) a custom-trained model. Not blocking for poster/report
  since the PC accuracy is within acceptable range of reference.

----------------------------------------------------
2026-04-30  Dilute Density Instability Fix
----------------------------------------------------

PROBLEM IDENTIFIED
  The single 10×10×10 patch approach for dilute density was seed-dependent:
    seed=42 : dilute=72.91  PC=6.048
    seed=1  : dilute=~73.7  PC=5.983
    seed=67 : dilute=~100.9 PC=4.372
  A 1.6x swing in PC from different random seeds is unacceptable for a
  reproducible pipeline. The Fabrini method specifies a manually chosen
  representative patch; our automated single-patch approximation was
  unstable because it depended entirely on where the shuffle landed first.

APPROACHES TESTED
  Single patch (seed=42)  : PC=6.048  -- unstable, seed-dependent
  All dilute voxels       : PC=4.769  -- stable but farther from reference
  50-patch average (final): PC=5.213  -- stable, closer to Fabrini spirit

FIX IMPLEMENTED (pipeline.py)
  Replaced single-patch search with average of up to 50 randomly sampled
  valid 10×10×10 patches across the nuclear dilute region (seed=42 for
  reproducible shuffle order). Falls back to all dilute voxels if fewer
  than 1 valid patch is found.
  Result is identical across repeated runs: PC=5.213, dilute=84.59.

CURRENT BEST RESULT (superseded — see 2026-04-30 entry below)
  PC = 5.213  (reference = 6.32, error = -17.5%)

----------------------------------------------------
2026-04-30  Connected-Component Nuclei Fix + Lowest-Patch Dilute Density
----------------------------------------------------

INSIGHT 1 — Connected-component nuclei relabeling
  The 76 Cellpose instance labels for nuclei collapse to only 16 truly
  connected 3D regions in the binary mask. Filtering to components
  >= 1000 voxels gives 5 clean nuclei (3 full + 2 partial at ROI edges).
  Binary pixel coverage barely changes (510,965 -> 510,809 voxels, -0.03%)
  so the PC formula is unaffected in terms of which pixels are "nuclear".
  Added as post-processing step inside segment_nuclei(): after Cellpose,
  re-label by 3D connected components (skimage.label) and drop noise.

INSIGHT 2 — Lowest-intensity patch selection for dilute density
  The 50-patch random average was sampling the full intensity distribution
  of the dilute region (dilute=84.59). A researcher manually picks the
  quietest representative spot. Sorting all 88,091 valid 10x10x10 patches
  by mean intensity and taking the lowest 50 is the automated equivalent.
  This is deterministic (no seed), stable, and biologically motivated.

  Bottom N patches vs PC:
    Bottom  10: dilute=69.83  PC=6.314
    Bottom  25: dilute=69.94  PC=6.304
    Bottom  50: dilute=70.03  PC=6.297  <- implemented
    Bottom 100: dilute=70.12  PC=6.288
    Random  50: dilute=78.06  PC=5.648  (previous)

FINAL RESULT (2026-04-30)
  PC = 6.297  (reference = 6.32, error = -0.4%)
  Nuclei: 5 (from 76 Cellpose labels via connected-component relabeling)
  Condensates: 186
  Condensate density: 440.93
  Dilute density    : 70.03  (lowest 50 patches)
  Background (B)    : 76.00

  Essentially matches the manual reference benchmark.

----------------------------------------------------
2026-04-30  Pipeline Summary — How It Works
----------------------------------------------------

PIPELINE STEPS

  [1/6] Load
    Reads two TIF Z-stacks: condensate channel (Ch2) and nuclei channel (Ch1).
    Each stack is 55 x 185 x 259 pixels.

  [2/6] Denoise
    Cellpose 3 DenoiseModel runs on every Z-slice before segmentation.
    Sharpens condensate boundaries without modifying the raw pixel values
    used for intensity measurements.

  [3/6] Segment
    Cellpose 3 (cyto3, do_3D=True) segments both channels in full 3D,
    tracking objects consistently across Z rather than slice-by-slice.
    Post-processing on nuclei: binary mask -> 3D connected components ->
    drop fragments < 1000 voxels, giving 5 clean nuclei from 76 raw labels.

  [4/6] Measure
    regionprops_table extracts area, centroid, and mean intensity per
    object per Z-slice.

  [5/6] 3D Volume
    Counts total voxels per label across all 55 slices.

  [6/6] Partition Coefficient (see below)

PARTITION COEFFICIENT FORMULA

  B            = minimum pixel value across the full 3D stack
                 (removes camera/autofluorescence background offset)

  cond_density = mean( clip(pixel - B, 0) )
                 over all voxels inside BOTH condensate mask AND nucleus mask

  dil_density  = mean( clip(pixel - B, 0) )
                 over the 50 lowest-intensity valid 10x10x10 patches
                 found entirely within the nuclear dilute region

  PC = cond_density / dil_density

  Interpretation: how much brighter are condensates compared to the
  surrounding nuclear background, after removing the camera offset.

THREE IMPROVEMENTS THAT BROUGHT PC FROM ~3.3 TO 6.297

  1. Background subtraction (added April 25)
     The reference was computed with B subtracted from all pixels before
     taking the ratio; the old pipeline was not doing this. Adding it was
     the largest single jump in PC.

  2. Connected-component nuclei relabeling (added April 30)
     Cellpose splits each nucleus into ~15-25 fragments due to internal
     condensate texture. Taking the binary mask (nuclei > 0) and running
     3D connected-component analysis collapses 76 Cellpose labels into
     5 true connected regions. This cleans up which pixels count as
     dilute phase without meaningfully changing the nuclear area covered.

  3. Lowest-intensity patch selection for dilute density (added April 30)
     The old approach picked a single random 10x10x10 patch — PC swung
     from 4.3 to 6.0 depending on the random seed. The fix: find all
     ~88,000 valid patches in the dilute region, sort by mean intensity,
     and average the 50 quietest. This is deterministic, stable, and
     matches how a researcher manually selects a quiet representative
     background spot.

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
TODO (2026-05-05)
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

  - Add discussion slides to presentation.
  - Add applicability of condensates slide to the introduction section.
